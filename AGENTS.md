# 本项目的本地交付约定

- 用户已授权：完成实现并通过构建、测试后，直接更新并重启 `/Applications/TabNest.app`，不只交付源码或构建产物。
- 用户已明确要求：后续替换不再备份旧应用或旧构建产物；需要回退时，通过回退代码重新编译。替换前确认安装目标及正在运行的进程；保留原 Bundle ID，不删除或重置站点配置、Cookie 和其他网站数据。
- 打包、签名验证成功后再退出旧进程并替换；安装后核对可执行文件与构建产物一致，并确认新进程启动。验证失败时不替换；安装失败时报告原因，不自动丢弃未提交的代码改动。
- 仅限本机开发交付；GitHub CI、DMG 和 Release 构建不自动安装到运行器。未通过验证时不替换。
- 本地安装不等于 Git 提交、推送或发布；这些动作仍需用户明确要求。
